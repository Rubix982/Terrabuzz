import React from "react";

// React components
import MainContent from '../components/Home/MainContent';

const Home = ({ children, style }) => {
  return (
    <MainContent />
  )
};

export default Home;
